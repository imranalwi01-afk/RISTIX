name: Deploy TechDocs

on:
  push:
    branches:
      - develop
    paths:
      - 'packages/docs/**'
      - 'packages/new-backend/src/**'
      - 'packages/frontend/src/hooks/**'
      - 'packages/frontend/src/utils/**'
      - '.github/workflows/docs-deploy.yml'

jobs:
  deploy:
    name: Build and Deploy Docs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install Dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Generate Code Documentation
        run: pnpm --filter docs run docs:generate

      - name: Build TechDocs
        run: pnpm --filter docs run build

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_IFRSPRO_9AF50 }}'
          projectId: ifrspro-9af50
          entryPoint: packages/docs
          channelId: live
