import { pgTable, date, varchar, numeric, integer, bigserial, timestamp, serial, bigint, doublePrecision, smallint, smallserial, boolean, char, text, check, pgView, pgSchema } from "drizzle-orm/pg-core"
import { sql } from "drizzle-orm"


// Legacy tables are in public schema of the legacy DB
// export const ifrs9 = pgSchema("ifrs9");





export const frs9AccountId = pgTable("frs9_account_id", {
	accountId: bigserial("account_id", { mode: "bigint" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	facilityNumber: varchar("facility_number", { length: 50 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	cifName: varchar("cif_name", { length: 150 }),
	createdby: varchar({ length: 36 }).default('SYSTEM'),
	createddate: timestamp({ mode: 'string' }).default(sql`CURRENT_TIMESTAMP`),
});



export const frs9AmortJournalData = pgTable("frs9_amort_journal_data", {
	id: bigserial({ mode: "bigint" }).notNull(),
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	facno: varchar({ length: 50 }),
	cifno: varchar({ length: 50 }),
	acctno: varchar({ length: 70 }),
	datasource: varchar({ length: 20 }),
	prdtype: varchar({ length: 20 }),
	prdcode: varchar({ length: 20 }),
	trxcode: varchar({ length: 20 }),
	ccy: varchar({ length: 10 }),
	journalcode: varchar({ length: 20 }),
	journalcode2: varchar({ length: 10 }),
	status: varchar({ length: 20 }),
	reverse: varchar({ length: 3 }),
	flagCf: varchar("flag_cf", { length: 3 }),
	drcr: varchar({ length: 2 }),
	glno: varchar({ length: 20 }),
	nAmount: numeric("n_amount", { precision: 32, scale: 6 }),
	nAmountIdr: numeric("n_amount_idr", { precision: 32, scale: 6 }),
	sourceprocess: varchar({ length: 20 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	intmid: bigint({ mode: "number" }),
	branch: varchar({ length: 20 }),
	noref: varchar({ length: 20 }),
	valctrCode: varchar("valctr_code", { length: 50 }),
	journalDesc: varchar("journal_desc", { length: 200 }),
	createddate: timestamp({ mode: 'string' }),
	createdby: varchar({ length: 30 }),
	glInternalCode: varchar("gl_internal_code", { length: 100 }),
	method: varchar({ length: 10 }),
	reservedVarchar1: varchar("reserved_varchar_1", { length: 100 }),
	reservedVarchar2: varchar("reserved_varchar_2", { length: 100 }),
	reservedVarchar3: varchar("reserved_varchar_3", { length: 100 }),
	glCostcenter: varchar("gl_costcenter", { length: 10 }),
});

export const frs9Default = pgTable("frs9_default", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	ruleId: bigint("rule_id", { mode: "number" }),
	osAtDefault: numeric("os_at_default", { precision: 32, scale: 6 }),
	eqvAtDefault: numeric("eqv_at_default", { precision: 32, scale: 6 }),
	eirAtDefault: doublePrecision("eir_at_default"),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }).defaultNow(),
});

export const frs9EclModelMapping = pgTable("frs9_ecl_model_mapping", {
	pkid: smallint().generatedAlwaysAsIdentity({ name: "frs9_ecl_model_mapping_pkid_seq", startWith: 1, increment: 1, minValue: 1, maxValue: 32767, cache: 1 }),
	eclModelId: smallint("ecl_model_id"),
	effDate: date("eff_date"),
	pfSegmentId: smallint("pf_segment_id"),
	pfSegmentName: varchar("pf_segment_name", { length: 36 }),
	pdModelId: smallint("pd_model_id"),
	lgdModelId: smallint("lgd_model_id"),
	eadModelId: smallint("ead_model_id"),
	ccfModelId: smallint("ccf_model_id"),
	ppModelId: smallint("pp_model_id"),
	ltModelId: smallint("lt_model_id"),
});

export const frs9EirEcf = pgTable("frs9_eir_ecf", {
	id: bigserial({ mode: "bigint" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	prcDate: date("prc_date"),
	nLoanAmt: numeric("n_loan_amt", { precision: 32, scale: 6 }),
	nIntRate: doublePrecision("n_int_rate"),
	nEffIntRate: doublePrecision("n_eff_int_rate"),
	startamortdate: date(),
	endamortdate: date(),
	gracedate: date(),
	disbPercentage: doublePrecision("disb_percentage"),
	disbAmount: numeric("disb_amount", { precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	paymentcode: varchar({ length: 3 }),
	intcalccode: varchar({ length: 3 }),
	paymentterm: varchar({ length: 3 }),
	isgrace: varchar({ length: 3 }),
	billingDate: date("billing_date"),
	prevPmtDate: date("prev_pmt_date"),
	pmtDate: date("pmt_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iDays: bigint("i_days", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iDays2: bigint("i_days2", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	counterRest: bigint("counter_rest", { mode: "number" }),
	counter: integer(),
	prevRestBalance: numeric("prev_rest_balance", { precision: 32, scale: 6 }),
	restBalance: numeric("rest_balance", { precision: 32, scale: 6 }),
	nOsprnPrev: numeric("n_osprn_prev", { precision: 32, scale: 6 }),
	nOsprn: numeric("n_osprn", { precision: 32, scale: 6 }),
	nInstallment: numeric("n_installment", { precision: 32, scale: 6 }),
	nPrnPayment: numeric("n_prn_payment", { precision: 32, scale: 6 }),
	nIntPayment: numeric("n_int_payment", { precision: 32, scale: 6 }),
	nAccruInt: numeric("n_accru_int", { precision: 32, scale: 6 }),
	nFairvaluePrev: numeric("n_fairvalue_prev", { precision: 32, scale: 6 }),
	nEffIntAmt: numeric("n_eff_int_amt", { precision: 32, scale: 6 }),
	nFairvalue: numeric("n_fairvalue", { precision: 32, scale: 6 }),
	nUnamortAmtPrev: numeric("n_unamort_amt_prev", { precision: 32, scale: 6 }),
	nAmortAmt: numeric("n_amort_amt", { precision: 32, scale: 6 }),
	nUnamortAmt: numeric("n_unamort_amt", { precision: 32, scale: 6 }),
	nCostUnamortAmtPrev: numeric("n_cost_unamort_amt_prev", { precision: 32, scale: 6 }),
	nCostAmortAmt: numeric("n_cost_amort_amt", { precision: 32, scale: 6 }),
	nCostUnamortAmt: numeric("n_cost_unamort_amt", { precision: 32, scale: 6 }),
	nFeeUnamortAmtPrev: numeric("n_fee_unamort_amt_prev", { precision: 32, scale: 6 }),
	nFeeAmortAmt: numeric("n_fee_amort_amt", { precision: 32, scale: 6 }),
	nFeeUnamortAmt: numeric("n_fee_unamort_amt", { precision: 32, scale: 6 }),
	nGainLossUnamortAmtPrev: numeric("n_gain_loss_unamort_amt_prev", { precision: 32, scale: 6 }),
	nGainLossAmortAmt: numeric("n_gain_loss_amort_amt", { precision: 32, scale: 6 }),
	nGainLossUnamortAmt: numeric("n_gain_loss_unamort_amt", { precision: 32, scale: 6 }),
	amortstopdate: date(),
	amortstopmsg: varchar({ length: 50 }),
	nDailyAmortCost: numeric("n_daily_amort_cost", { precision: 32, scale: 6 }),
	nDailyAmortFee: numeric("n_daily_amort_fee", { precision: 32, scale: 6 }),
	nDailyGainLoss: numeric("n_daily_gain_loss", { precision: 32, scale: 6 }),
	nEffIntAmt0: numeric("n_eff_int_amt0", { precision: 32, scale: 6 }),
	nEffIntRate0: doublePrecision("n_eff_int_rate0"),
	nDailyIntAdjAmt: numeric("n_daily_int_adj_amt", { precision: 32, scale: 6 }),
	nIntAdjAmt: numeric("n_int_adj_amt", { precision: 32, scale: 6 }),
	swAdjCost: numeric("sw_adj_cost", { precision: 32, scale: 6 }),
	swAdjFee: numeric("sw_adj_fee", { precision: 32, scale: 6 }),
	npvRate: numeric("npv_rate", { precision: 14, scale: 10 }),
	npvAmount: numeric("npv_amount", { precision: 32, scale: 6 }),
	nocfOsprn: numeric("nocf_osprn", { precision: 32, scale: 6 }),
	nocfOsprnPrev: numeric("nocf_osprn_prev", { precision: 32, scale: 6 }),
	nocfIntRate: doublePrecision("nocf_int_rate"),
	nocfPrnPayment: numeric("nocf_prn_payment", { precision: 32, scale: 6 }),
	nocfEffIntAmt: numeric("nocf_eff_int_amt", { precision: 32, scale: 6 }),
	nocfUnamortAmtPrev: numeric("nocf_unamort_amt_prev", { precision: 32, scale: 6 }),
	nocfAmortAmt: numeric("nocf_amort_amt", { precision: 32, scale: 6 }),
	nocfUnamortAmt: numeric("nocf_unamort_amt", { precision: 32, scale: 6 }),
});

export const frs9EventChanges = pgTable("frs9_event_changes", {
	prcDate: timestamp("prc_date", { mode: 'string' }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountNumber: varchar("account_number", { length: 70 }),
	effectiveDate: timestamp("effective_date", { mode: 'string' }),
	beforeValue: varchar("before_value", { length: 100 }),
	afterValue: varchar("after_value", { length: 100 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eventId: bigint("event_id", { mode: "number" }),
	remarks: varchar({ length: 100 }),
	createdby: varchar({ length: 50 }),
	createdDate: timestamp("created_date", { mode: 'string' }),
});

export const frs9ImpCaAccountEvent = pgTable("frs9_imp_ca_account_event", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	eventId: smallint("event_id"),
	oldValue: varchar("old_value", { length: 100 }),
	newValue: varchar("new_value", { length: 100 }),
	remarks: varchar({ length: 100 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaAccountEventPrv = pgTable("frs9_imp_ca_account_event_prv", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	eventId: smallint("event_id"),
	oldValue: varchar("old_value", { length: 100 }),
	newValue: varchar("new_value", { length: 100 }),
	remarks: varchar({ length: 100 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaEad = pgTable("frs9_imp_ca_ead", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountNumber: varchar("account_number", { length: 50 }),
	mob: smallint(),
	idays: smallint(),
	paymentDate: date("payment_date"),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	installment: numeric({ precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	eadAmt: numeric("ead_amt", { precision: 32, scale: 6 }),
});

export const frs9ImpCaEadConfig = pgTable("frs9_imp_ca_ead_config", {
	pkid: smallserial().notNull(),
	eadModelName: varchar("ead_model_name", { length: 250 }),
	segmentId: smallint("segment_id"),
	eadMethod: varchar("ead_method", { length: 10 }),
	calcMethod: varchar("calc_method", { length: 10 }),
	activeFlag: boolean("active_flag").notNull(),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaEadPaymAvg = pgTable("frs9_imp_ca_ead_paym_avg", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	tenor: smallint(),
	counter: smallint(),
	paymAvg: doublePrecision("paym_avg"),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaEclConfigd = pgTable("frs9_imp_ca_ecl_configd", {
	pkid: smallserial().notNull(),
	eclModelId: smallint("ecl_model_id"),
	pfSegmentId: smallint("pf_segment_id"),
	stageRuleId: smallint("stage_rule_id"),
	pdModelId: smallint("pd_model_id"),
	lgdModelId: smallint("lgd_model_id"),
	eadModelId: smallint("ead_model_id"),
	overlayRate: smallint("overlay_rate").default(100),
	periodType: smallint("period_type"),
	periodDate: date("period_date"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaEclConfigh = pgTable("frs9_imp_ca_ecl_configh", {
	pkid: smallserial().notNull(),
	eclModelName: varchar("ecl_model_name", { length: 50 }),
	module: varchar({ length: 10 }),
	effectiveDate: date("effective_date").notNull(),
	activeFlag: boolean("active_flag").notNull(),
	lastRunPeriod: date("last_run_period"),
	lastRunStatus: varchar("last_run_status", { length: 50 }),
	lastRunDate: timestamp("last_run_date", { mode: 'string' }),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaEclDetail = pgTable("frs9_imp_ca_ecl_detail", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	branchCode: varchar("branch_code", { length: 50 }),
	prdCode: varchar("prd_code", { length: 20 }),
	remainingTenor: smallint("remaining_tenor"),
	expectedLife: smallint("expected_life"),
	interestRate: doublePrecision("interest_rate"),
	effInterestRate: doublePrecision("eff_interest_rate"),
	outstanding: numeric({ precision: 32, scale: 6 }),
	unusedAmt: numeric("unused_amt", { precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	installmentAmt: numeric("installment_amt", { precision: 32, scale: 6 }),
	impairedFlag: boolean("impaired_flag"),
	dpd: smallint(),
	collectability: smallint(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	stage: varchar({ length: 5 }),
	bucketId: smallint("bucket_id"),
	eclModelId: smallint("ecl_model_id").notNull(),
	segmentId: smallint("segment_id"),
	eclAmtCaOnbs: numeric("ecl_amt_ca_onbs", { precision: 38, scale: 6 }),
	eclAmtCaOffbs: numeric("ecl_amt_ca_offbs", { precision: 38, scale: 6 }),
});

export const frs9ImpCaEclSum = pgTable("frs9_imp_ca_ecl_sum", {
	prcDate: date("prc_date"),
	eclModelId: smallint("ecl_model_id").notNull(),
	segmentId: smallint("segment_id"),
	branchCode: varchar("branch_code", { length: 50 }),
	prdCode: varchar("prd_code", { length: 20 }),
	stage: varchar({ length: 5 }),
	bucketId: smallint("bucket_id"),
	impairedFlag: boolean("impaired_flag"),
	outstanding: numeric({ precision: 38, scale: 6 }),
	unusedAmt: numeric("unused_amt", { precision: 38, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 38, scale: 6 }),
	eclAmtCaOnbs: numeric("ecl_amt_ca_onbs", { precision: 38, scale: 6 }),
	eclAmtCaOffbs: numeric("ecl_amt_ca_offbs", { precision: 38, scale: 6 }),
});

export const frs9ImpCaEclTs = pgTable("frs9_imp_ca_ecl_ts", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountNumber: varchar("account_number", { length: 50 }),
	eclModelId: smallint("ecl_model_id").notNull(),
	segmentId: smallint("segment_id"),
	paymentDate: date("payment_date"),
	mob: smallint(),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	eadOnbs: numeric("ead_onbs", { precision: 32, scale: 6 }),
	eadOffbs: numeric("ead_offbs", { precision: 32, scale: 6 }),
	pdRate: doublePrecision("pd_rate"),
	lgdRate: doublePrecision("lgd_rate"),
	ccfRate: doublePrecision("ccf_rate"),
	prepaymentRate: doublePrecision("prepayment_rate"),
	discountRate: doublePrecision("discount_rate"),
	eclAmtCaOnbs: numeric("ecl_amt_ca_onbs", { precision: 32, scale: 6 }),
	eclAmtCaOffbs: numeric("ecl_amt_ca_offbs", { precision: 32, scale: 6 }),
});

export const frs9ImpCaFlScalard = pgTable("frs9_imp_ca_fl_scalard", {
	pkid: smallserial().notNull(),
	scalarId: smallint("scalar_id"),
	period: smallint(),
	weightedScalar: doublePrecision("weighted_scalar"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaFlScalarh = pgTable("frs9_imp_ca_fl_scalarh", {
	pkid: smallserial().notNull(),
	scalarName: varchar("scalar_name", { length: 30 }),
	activeFlag: boolean("active_flag").notNull(),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaLgd = pgTable("frs9_imp_ca_lgd", {
	pkid: smallint().generatedAlwaysAsIdentity({ name: "frs9_imp_ca_lgd_pkid_seq", startWith: 1, increment: 1, minValue: 1, maxValue: 32767, cache: 1 }),
	prcDate: date("prc_date"),
	lgdModelId: smallint("lgd_model_id"),
	lgdModelName: varchar("lgd_model_name", { length: 100 }),
	osDefault: numeric("os_default", { precision: 32, scale: 6 }),
	eqvOsDefault: numeric("eqv_os_default", { precision: 32, scale: 6 }),
	recoveryAmt: numeric("recovery_amt", { precision: 32, scale: 6 }),
	eqvRecoveryAmt: numeric("eqv_recovery_amt", { precision: 32, scale: 6 }),
	pvRecoveryAmt: numeric("pv_recovery_amt", { precision: 32, scale: 6 }),
	pvEqvRecoveryAmt: numeric("pv_eqv_recovery_amt", { precision: 32, scale: 6 }),
	recoveryRate: doublePrecision("recovery_rate"),
	lgdRate: doublePrecision("lgd_rate"),
});

export const frs9ImpCaLgdCollData = pgTable("frs9_imp_ca_lgd_coll_data", {
	prcDate: date("prc_date"),
	pvDate: date("pv_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	soldFlag: boolean("sold_flag"),
	collaAmt: numeric("colla_amt", { precision: 32, scale: 6 }),
	sellAmt: numeric("sell_amt", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaLgdConfig = pgTable("frs9_imp_ca_lgd_config", {
	pkid: smallserial().notNull(),
	lgdModelName: varchar("lgd_model_name", { length: 250 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdMethod: bigint("lgd_method", { mode: "number" }),
	populationType: varchar("population_type", { length: 10 }),
	observationPeriod: varchar("observation_period", { length: 50 }),
	observationStartDate: date("observation_start_date"),
	workoutPeriod: integer("workout_period"),
	flFlag: boolean("fl_flag").notNull(),
	flScalarId: smallint("fl_scalar_id"),
	lgdRate: doublePrecision("lgd_rate"),
	activeFlag: boolean("active_flag"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaLgdD = pgTable("frs9_imp_ca_lgd_d", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdMethod: bigint("lgd_method", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	eqvOs: numeric("eqv_os", { precision: 32, scale: 6 }),
	eqvRec: numeric("eqv_rec", { precision: 32, scale: 6 }),
	npvEqvRec: numeric("npv_eqv_rec", { precision: 32, scale: 6 }),
	recRate: doublePrecision("rec_rate"),
	lgd: doublePrecision(),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaLgdData = pgTable("frs9_imp_ca_lgd_data", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdMethod: bigint("lgd_method", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	eqvAtDefault: numeric("eqv_at_default", { precision: 32, scale: 6 }),
	woDate: date("wo_date"),
	closedDate: date("closed_date"),
	accountStatus: char("account_status", { length: 1 }),
	eqvWo: numeric("eqv_wo", { precision: 32, scale: 6 }),
	eirAtDefault: doublePrecision("eir_at_default"),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
	updatedby: varchar({ length: 100 }),
	updateddate: timestamp({ mode: 'string' }),
	repoDate: date("repo_date"),
	eqvRepo: numeric("eqv_repo", { precision: 32, scale: 6 }),
});

export const frs9ImpCaLgdH = pgTable("frs9_imp_ca_lgd_h", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdMethod: bigint("lgd_method", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	modelId: bigint("model_id", { mode: "number" }),
	eqvOs: numeric("eqv_os", { precision: 32, scale: 6 }),
	eqvRec: numeric("eqv_rec", { precision: 32, scale: 6 }),
	npvEqvRec: numeric("npv_eqv_rec", { precision: 32, scale: 6 }),
	recRate: doublePrecision("rec_rate"),
	lgd: doublePrecision(),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaLgdRecD = pgTable("frs9_imp_ca_lgd_rec_d", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdMethod: bigint("lgd_method", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	eqvOs: numeric("eqv_os", { precision: 32, scale: 6 }),
	eir: doublePrecision(),
	seq: smallint(),
	eqvRec: numeric("eqv_rec", { precision: 32, scale: 6 }),
	npvEqvRec: numeric("npv_eqv_rec", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }),
	rn: integer(),
});

export const frs9ImpCaLgdRecData = pgTable("frs9_imp_ca_lgd_rec_data", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	defaultRuleId: bigint("default_rule_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	recOs: numeric("rec_os", { precision: 32, scale: 6 }),
	eqvRecOs: numeric("eqv_rec_os", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdConfig = pgTable("frs9_imp_ca_pd_config", {
	pkid: smallserial().notNull(),
	pdModelName: varchar("pd_model_name", { length: 250 }),
	segmentId: smallint("segment_id"),
	pdMethod: varchar("pd_method", { length: 10 }).notNull(),
	interval: smallint().notNull(),
	populationType: varchar("population_type", { length: 10 }),
	observationPeriod: smallint("observation_period").notNull(),
	observationStartDate: date("observation_start_date"),
	multiplication: smallint(),
	flFlag: boolean("fl_flag"),
	flScalarId: smallint("fl_scalar_id"),
	iaFlag: boolean("ia_flag").notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	activeFlag: boolean("active_flag").notNull(),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaPdEnr = pgTable("frs9_imp_ca_pd_enr", {
	prcDate: date("prc_date").notNull(),
	baseDate: date("base_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }).notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketFrom: smallint("bucket_from"),
	bucketTo: smallint("bucket_to"),
	calcAmount: numeric("calc_amount", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdFlowrate = pgTable("frs9_imp_ca_pd_flowrate", {
	prcDate: date("prc_date").notNull(),
	baseDate: date("base_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }).notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketFrom: smallint("bucket_from"),
	bucketTo: smallint("bucket_to"),
	flowrate: doublePrecision(),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdFlowrateAvg = pgTable("frs9_imp_ca_pd_flowrate_avg", {
	prcDate: date("prc_date").notNull(),
	baseDate: date("base_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }).notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketFrom: smallint("bucket_from"),
	bucketTo: smallint("bucket_to"),
	flowrate: doublePrecision(),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdMigration = pgTable("frs9_imp_ca_pd_migration", {
	prcDate: date("prc_date").notNull(),
	baseDate: date("base_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }).notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	bucketFrom: smallint("bucket_from"),
	bucketTo: smallint("bucket_to"),
	calcAmount: numeric("calc_amount", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdMmult = pgTable("frs9_imp_ca_pd_mmult", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	flSeq: smallint("fl_seq"),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketFrom: smallint("bucket_from"),
	bucketTo: smallint("bucket_to"),
	mmult: doublePrecision(),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdOdr = pgTable("frs9_imp_ca_pd_odr", {
	prcDate: date("prc_date"),
	baseDate: date("base_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	totDefault: bigint("tot_default", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	nonDefault: bigint("non_default", { mode: "number" }),
	odr: doublePrecision(),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdProxy = pgTable("frs9_imp_ca_pd_proxy", {
	prcDate: date("prc_date").notNull(),
	baseDate: date("base_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }).notNull(),
	bucketGroup: varchar("bucket_group", { length: 50 }),
	bucketFrom: smallint("bucket_from"),
	bucketTo: smallint("bucket_to"),
	pd: doublePrecision(),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaPdStructure = pgTable("frs9_imp_ca_pd_structure", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	pdMethod: smallint("pd_method"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	scalarId: bigint("scalar_id", { mode: "number" }),
	scenarioNo: integer("scenario_no"),
	bucketGroup: varchar("bucket_group", { length: 100 }),
	bucketId: integer("bucket_id"),
	flSeq: integer("fl_seq"),
	flYear: integer("fl_year"),
	flMonth: integer("fl_month"),
	pdNonFl: doublePrecision("pd_non_fl"),
	pd: doublePrecision(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdby: varchar({ length: 50 }),
	weightedScalar: doublePrecision("weighted_scalar"),
});

export const frs9ImpCaPdTs = pgTable("frs9_imp_ca_pd_ts", {
	pkid: smallint().generatedAlwaysAsIdentity({ name: "frs9_imp_ca_pd_ts_pkid_seq", startWith: 1, increment: 1, minValue: 1, maxValue: 32767, cache: 1 }),
	prcDate: date("prc_date").notNull(),
	pdModelId: smallint("pd_model_id").notNull(),
	pdModelName: varchar("pd_model_name", { length: 100 }),
	bucketId: smallint("bucket_id"),
	pdYear: smallint("pd_year"),
	pdMonth: smallint("pd_month"),
	pdSequence: smallint("pd_sequence"),
	pdDate: date("pd_date"),
	pdRate: doublePrecision("pd_rate"),
});

export const frs9ImpCaResultD = pgTable("frs9_imp_ca_result_d", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	facilityNumber: varchar("facility_number", { length: 100 }),
	cifNumber: varchar("cif_number", { length: 100 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	remainingTenor: smallint("remaining_tenor"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	defaultFlag: boolean("default_flag"),
	dpd: smallint(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eclModelId: bigint("ecl_model_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadConfigId: bigint("ead_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadMethod: bigint("ead_method", { mode: "number" }),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketId: integer("bucket_id"),
	currency: varchar({ length: 5 }),
	stage: smallint(),
	scenarioNo: smallint("scenario_no"),
	flSeq: smallint("fl_seq"),
	flYear: smallint("fl_year"),
	flMotnh: smallint("fl_motnh"),
	eir: doublePrecision(),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	unamortCostAmt: numeric("unamort_cost_amt", { precision: 32, scale: 6 }),
	unamortFeeAmt: numeric("unamort_fee_amt", { precision: 32, scale: 6 }),
	eadBalance: numeric("ead_balance", { precision: 32, scale: 6 }),
	paymAvg: doublePrecision("paym_avg"),
	principalAmt: numeric("principal_amt", { precision: 32, scale: 6 }),
	sumPrincipalAmt: numeric("sum_principal_amt", { precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	sumNextInterest: numeric("sum_next_interest", { precision: 32, scale: 6 }),
	ead: numeric({ precision: 32, scale: 6 }),
	pd: doublePrecision(),
	lgd: doublePrecision(),
	eclAmount: numeric("ecl_amount", { precision: 32, scale: 6 }),
	probability: doublePrecision(),
	eclWeighted: numeric("ecl_weighted", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaResultDPrv = pgTable("frs9_imp_ca_result_d_prv", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	facilityNumber: varchar("facility_number", { length: 100 }),
	cifNumber: varchar("cif_number", { length: 100 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	remainingTenor: smallint("remaining_tenor"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	defaultFlag: boolean("default_flag"),
	dpd: smallint(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eclModelId: bigint("ecl_model_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadConfigId: bigint("ead_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadMethod: bigint("ead_method", { mode: "number" }),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketId: integer("bucket_id"),
	currency: varchar({ length: 5 }),
	stage: smallint(),
	scenarioNo: smallint("scenario_no"),
	flSeq: smallint("fl_seq"),
	flYear: smallint("fl_year"),
	flMotnh: smallint("fl_motnh"),
	eir: doublePrecision(),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	unamortCostAmt: numeric("unamort_cost_amt", { precision: 32, scale: 6 }),
	unamortFeeAmt: numeric("unamort_fee_amt", { precision: 32, scale: 6 }),
	eadBalance: numeric("ead_balance", { precision: 32, scale: 6 }),
	paymAvg: doublePrecision("paym_avg"),
	principalAmt: numeric("principal_amt", { precision: 32, scale: 6 }),
	sumPrincipalAmt: numeric("sum_principal_amt", { precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	sumNextInterest: numeric("sum_next_interest", { precision: 32, scale: 6 }),
	ead: numeric({ precision: 32, scale: 6 }),
	pd: doublePrecision(),
	lgd: doublePrecision(),
	eclAmount: numeric("ecl_amount", { precision: 32, scale: 6 }),
	probability: doublePrecision(),
	eclWeighted: numeric("ecl_weighted", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaResultH = pgTable("frs9_imp_ca_result_h", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	facilityNumber: varchar("facility_number", { length: 100 }),
	cifNumber: varchar("cif_number", { length: 100 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	remainingTenor: smallint("remaining_tenor"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	defaultFlag: boolean("default_flag"),
	dpd: smallint(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eclModelId: bigint("ecl_model_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	lgd: doublePrecision(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadConfigId: bigint("ead_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadMethod: bigint("ead_method", { mode: "number" }),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketId: integer("bucket_id"),
	currency: varchar({ length: 5 }),
	stage: smallint(),
	eir: doublePrecision(),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	unamortCostAmt: numeric("unamort_cost_amt", { precision: 32, scale: 6 }),
	unamortFeeAmt: numeric("unamort_fee_amt", { precision: 32, scale: 6 }),
	eclAmount: numeric("ecl_amount", { precision: 32, scale: 6 }),
	overlayAmount: numeric("overlay_amount", { precision: 32, scale: 6 }),
	eclFinal: numeric("ecl_final", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaResultHPrv = pgTable("frs9_imp_ca_result_h_prv", {
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	facilityNumber: varchar("facility_number", { length: 100 }),
	cifNumber: varchar("cif_number", { length: 100 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	remainingTenor: smallint("remaining_tenor"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	defaultFlag: boolean("default_flag"),
	dpd: smallint(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eclModelId: bigint("ecl_model_id", { mode: "number" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	lgd: doublePrecision(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadConfigId: bigint("ead_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadMethod: bigint("ead_method", { mode: "number" }),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketId: integer("bucket_id"),
	currency: varchar({ length: 5 }),
	stage: smallint(),
	eir: doublePrecision(),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	unamortCostAmt: numeric("unamort_cost_amt", { precision: 32, scale: 6 }),
	unamortFeeAmt: numeric("unamort_fee_amt", { precision: 32, scale: 6 }),
	eclAmount: numeric("ecl_amount", { precision: 32, scale: 6 }),
	overlayAmount: numeric("overlay_amount", { precision: 32, scale: 6 }),
	eclFinal: numeric("ecl_final", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaScenarioData = pgTable("frs9_imp_ca_scenario_data", {
	prcDate: date("prc_date").notNull(),
	segmentId: integer("segment_id").notNull(),
	segmentType: varchar("segment_type", { length: 100 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	accountStatus: varchar("account_status", { length: 20 }),
	currency: varchar({ length: 5 }),
	dpd: smallint(),
	collectability: smallint(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	defaultFlag: boolean("default_flag"),
	impairedStatus: char("impaired_status", { length: 1 }),
	tenor: smallint(),
	remainingTenor: smallint("remaining_tenor"),
	paymentFreq: smallint("payment_freq"),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
	createdhost: varchar({ length: 50 }),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ImpCaSchd = pgTable("frs9_imp_ca_schd", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountNumber: varchar("account_number", { length: 50 }),
	mob: smallint(),
	idays: smallint(),
	paymentDate: date("payment_date"),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	installment: numeric({ precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaSchdHist = pgTable("frs9_imp_ca_schd_hist", {
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }),
	mob: smallint(),
	idays: smallint(),
	paymentDate: date("payment_date").notNull(),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	installment: numeric({ precision: 33, scale: 6 }),
	endPrcDate: date("end_prc_date"),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaSchdPrv = pgTable("frs9_imp_ca_schd_prv", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }),
	mob: smallint(),
	idays: smallint(),
	paymentDate: date("payment_date").notNull(),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	installment: numeric({ precision: 33, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpCaSegmentQuery = pgTable("frs9_imp_ca_segment_query", {
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }).notNull(),
	groupSegment: varchar("group_segment", { length: 50 }),
	subSegment: varchar("sub_segment", { length: 50 }),
	segment: varchar({ length: 50 }),
	tableName: varchar("table_name", { length: 30 }),
	condition: text(),
	segmentType: varchar("segment_type", { length: 50 }),
});

export const frs9ImpIaDcf = pgTable("frs9_imp_ia_dcf", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iaId: bigint("ia_id", { mode: "number" }).notNull(),
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	mob: bigint({ mode: "number" }).notNull(),
	periode: date().notNull(),
	principal: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	interest: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	collateral: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	status: char({ length: 1 }).notNull(),
	createdby: varchar({ length: 36 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 36 }).notNull(),
	updatedby: varchar({ length: 36 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 30 }),
});

export const frs9ImpIaDetail = pgTable("frs9_imp_ia_detail", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iaId: bigint("ia_id", { mode: "number" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	effInterestRate: doublePrecision("eff_interest_rate").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	mob: bigint({ mode: "number" }).notNull(),
	periode: date().notNull(),
	principal: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	interest: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	installment: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	collateral: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	poRate1: doublePrecision("po_rate_1").default(0).notNull(),
	rrRate1: doublePrecision("rr_rate_1").default(0).notNull(),
	default1: numeric("default_1", { precision: 32, scale: 6 }).default('0').notNull(),
	poRate2: doublePrecision("po_rate_2").default(0).notNull(),
	rrRate2: doublePrecision("rr_rate_2").default(0).notNull(),
	default2: numeric("default_2", { precision: 32, scale: 6 }).default('0').notNull(),
	poRate3: doublePrecision("po_rate_3").default(0).notNull(),
	rrRate3: doublePrecision("rr_rate_3").default(0).notNull(),
	default3: numeric("default_3", { precision: 32, scale: 6 }).default('0').notNull(),
	pwAmt: numeric("pw_amt", { precision: 32, scale: 6 }).default('0').notNull(),
	discountFactor: doublePrecision("discount_factor").notNull(),
	pvAmt: numeric("pv_amt", { precision: 32, scale: 6 }).default('0').notNull(),
	beginningBalance: numeric("beginning_balance", { precision: 32, scale: 6 }).default('0').notNull(),
	eirAmt: numeric("eir_amt", { precision: 32, scale: 6 }).default('0').notNull(),
	endingBalance: numeric("ending_balance", { precision: 32, scale: 6 }).default('0').notNull(),
	createdby: varchar({ length: 36 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 36 }).notNull(),
	updatedby: varchar({ length: 36 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 30 }),
});

export const frs9ImpIaHeader = pgTable("frs9_imp_ia_header", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iaId: bigint("ia_id", { mode: "number" }).notNull(),
	prcDate: date("prc_date").notNull(),
	effDate: date("eff_date").notNull(),
	cifNumber: varchar("cif_number", { length: 50 }).notNull(),
	cifName: varchar("cif_name", { length: 150 }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	currency: varchar({ length: 5 }).notNull(),
	effInterestRate: doublePrecision("eff_interest_rate").notNull(),
	interestRate: doublePrecision("interest_rate").notNull(),
	dpd: smallint(),
	collectability: smallint(),
	ratingCode: varchar("rating_code", { length: 5 }),
	impairedFlag: char("impaired_flag", { length: 1 }).notNull(),
	method: varchar({ length: 20 }),
	plafond: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	outstanding: numeric({ precision: 32, scale: 6 }).default('0').notNull(),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }).default('0').notNull(),
	carryingAmt: numeric("carrying_amt", { precision: 32, scale: 6 }).default('0').notNull(),
	eadAmt: numeric("ead_amt", { precision: 32, scale: 6 }).default('0').notNull(),
	pvDcfAmt: numeric("pv_dcf_amt", { precision: 32, scale: 6 }).default('0').notNull(),
	eclIaAmt: numeric("ecl_ia_amt", { precision: 32, scale: 6 }).default('0').notNull(),
	triggerRemarks: varchar("trigger_remarks", { length: 1000 }),
	triggerFilename: varchar("trigger_filename", { length: 100 }),
	scenarioId: smallint("scenario_id"),
	nOfScenario: smallint("n_of_scenario"),
	poRate1: doublePrecision("po_rate_1").default(0).notNull(),
	poRate2: doublePrecision("po_rate_2").default(0).notNull(),
	poRate3: doublePrecision("po_rate_3").default(0).notNull(),
	scName1: varchar("sc_name_1", { length: 20 }),
	scName2: varchar("sc_name_2", { length: 20 }),
	scName3: varchar("sc_name_3", { length: 20 }),
	status: integer().notNull(),
	createdby: varchar({ length: 36 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 36 }).notNull(),
	updatedby: varchar({ length: 36 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 30 }),
	reviewedby: varchar({ length: 36 }),
	revieweddate: timestamp({ mode: 'string' }),
	reviewedhost: varchar({ length: 30 }),
});

export const frs9ImpIaResultD = pgTable("frs9_imp_ia_result_d", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iaId: bigint("ia_id", { mode: "number" }),
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	mob: smallint(),
	periode: date(),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	installment: numeric({ precision: 32, scale: 6 }),
	collateral: numeric({ precision: 32, scale: 6 }),
	poRate1: doublePrecision("po_rate_1"),
	rrRate1: doublePrecision("rr_rate_1"),
	default1: numeric("default_1", { precision: 32, scale: 6 }),
	poRate2: doublePrecision("po_rate_2"),
	rrRate2: doublePrecision("rr_rate_2"),
	default2: numeric("default_2", { precision: 32, scale: 6 }),
	poRate3: doublePrecision("po_rate_3"),
	rrRate3: doublePrecision("rr_rate_3"),
	default3: numeric("default_3", { precision: 32, scale: 6 }),
	pwAmt: numeric("pw_amt", { precision: 32, scale: 6 }),
	discountFactor: doublePrecision("discount_factor"),
	pvAmt: numeric("pv_amt", { precision: 32, scale: 6 }),
	beginningBalance: numeric("beginning_balance", { precision: 32, scale: 6 }),
	eirAmt: numeric("eir_amt", { precision: 32, scale: 6 }),
	endingBalance: numeric("ending_balance", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpIaResultH = pgTable("frs9_imp_ia_result_h", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iaId: bigint("ia_id", { mode: "number" }),
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountNumber: varchar("account_number", { length: 50 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	cifName: varchar("cif_name", { length: 150 }),
	currency: varchar({ length: 5 }),
	dpd: smallint(),
	collectability: smallint(),
	ratingCode: varchar("rating_code", { length: 5 }),
	interestRate: doublePrecision("interest_rate"),
	effInterestRate: doublePrecision("eff_interest_rate"),
	outstanding: numeric({ precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	carryingAmt: numeric("carrying_amt", { precision: 32, scale: 6 }),
	eadAmt: numeric("ead_amt", { precision: 32, scale: 6 }),
	pvDcfAmt: numeric("pv_dcf_amt", { precision: 32, scale: 6 }),
	eclIaAmt: numeric("ecl_ia_amt", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9ImpIaRr = pgTable("frs9_imp_ia_rr", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	iaId: bigint("ia_id", { mode: "number" }).notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	periodStart: date("period_start").notNull(),
	periodEnd: date("period_end").notNull(),
	rrRate1: doublePrecision("rr_rate_1").default(0).notNull(),
	rrRate2: doublePrecision("rr_rate_2").default(0).notNull(),
	rrRate3: doublePrecision("rr_rate_3").default(0).notNull(),
	createdby: varchar({ length: 36 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 36 }).notNull(),
	updatedby: varchar({ length: 36 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 30 }),
});

export const frs9ImpJournalData = pgTable("frs9_imp_journal_data", {
	id: bigserial({ mode: "bigint" }).notNull(),
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	prdcode: varchar({ length: 50 }),
	currency: varchar({ length: 5 }),
	journalcode: varchar({ length: 50 }),
	journalcode2: varchar({ length: 50 }),
	reverse: boolean(),
	flagCf: varchar("flag_cf", { length: 10 }),
	dbcr: varchar({ length: 2 }),
	glNumber: varchar("gl_number", { length: 20 }),
	nAmount: numeric("n_amount", { precision: 32, scale: 6 }),
	nAmountIdr: numeric("n_amount_idr", { precision: 32, scale: 6 }),
	sourceprocess: varchar({ length: 50 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	intmid: bigint({ mode: "number" }),
	branch: varchar({ length: 50 }),
	noref: varchar({ length: 20 }),
	valctrCode: varchar("valctr_code", { length: 50 }),
	glDesc: varchar("gl_desc", { length: 200 }),
	glCostcenter: varchar("gl_costcenter", { length: 20 }),
	createddate: timestamp({ mode: 'string' }),
	createdby: varchar({ length: 30 }),
});

export const frs9ImpMovementData = pgTable("frs9_imp_movement_data", {
	prcDate: date("prc_date"),
	urut: integer(),
	groupSegment: varchar("group_segment", { length: 100 }),
	stage1: numeric({ precision: 32, scale: 6 }),
	stage2: numeric({ precision: 32, scale: 6 }),
	stage3: numeric({ precision: 32, scale: 6 }),
	stage1I: numeric("stage1_i", { precision: 32, scale: 6 }),
	stage2I: numeric("stage2_i", { precision: 32, scale: 6 }),
	stage3I: numeric("stage3_i", { precision: 32, scale: 6 }),
	gcaStage1: numeric("gca_stage1", { precision: 32, scale: 6 }),
	gcaStage2: numeric("gca_stage2", { precision: 32, scale: 6 }),
	gcaStage3: numeric("gca_stage3", { precision: 32, scale: 6 }),
	gcaStage1I: numeric("gca_stage1_i", { precision: 32, scale: 6 }),
	gcaStage2I: numeric("gca_stage2_i", { precision: 32, scale: 6 }),
	gcaStage3I: numeric("gca_stage3_i", { precision: 32, scale: 6 }),
	poci: numeric({ precision: 32, scale: 6 }),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
	createdhost: varchar({ length: 30 }),
});

export const frs9ImpNominative = pgTable("frs9_imp_nominative", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	dataSource: varchar("data_source", { length: 50 }),
	accountNumber: varchar("account_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	cifName: varchar("cif_name", { length: 150 }),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	outstanding: numeric({ precision: 32, scale: 6 }),
	currency: varchar({ length: 5 }),
	interestRate: doublePrecision("interest_rate"),
	dpd: smallint(),
	extRating: varchar("ext_rating", { length: 5 }),
	tenor: smallint(),
	prdCode: varchar("prd_code", { length: 20 }),
	groupSegment: varchar("group_segment", { length: 50 }),
	segment: varchar({ length: 50 }),
	subSegment: varchar("sub_segment", { length: 50 }),
	ratingBucket: smallint("rating_bucket"),
	lgd: doublePrecision(),
	sicr: boolean(),
	stage: smallint(),
	ecl: numeric({ precision: 32, scale: 6 }),
	eclCoverage: doublePrecision("ecl_coverage"),
});

export const frs9MasterAccount = pgTable("frs9_master_account", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	facilityNumber: varchar("facility_number", { length: 50 }),
	cifType: varchar("cif_type", { length: 50 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	cifName: varchar("cif_name", { length: 150 }),
	accountStatus: varchar("account_status", { length: 20 }),
	dataSource: varchar("data_source", { length: 20 }),
	prdGroup: varchar("prd_group", { length: 20 }),
	prdType: varchar("prd_type", { length: 50 }),
	prdCode: varchar("prd_code", { length: 20 }),
	glGroup: varchar("gl_group", { length: 50 }),
	branchCode: varchar("branch_code", { length: 50 }),
	tenorOrg: smallint("tenor_org"),
	remainingTenor: smallint("remaining_tenor"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	startAmortDate: date("start_amort_date"),
	endAmortDate: date("end_amort_date"),
	paidOffDate: date("paid_off_date"),
	writeOffDate: date("write_off_date"),
	firstPaymentDate: date("first_payment_date"),
	nextPaymentDate: date("next_payment_date"),
	lastPaymentDate: date("last_payment_date"),
	nextSchPrinDate: date("next_sch_prin_date"),
	nextSchIntDate: date("next_sch_int_date"),
	graceType: varchar("grace_type", { length: 5 }),
	graceStartDate: date("grace_start_date"),
	graceEndDate: date("grace_end_date"),
	marketRate: doublePrecision("market_rate"),
	interestRate: doublePrecision("interest_rate"),
	effInterestRate: doublePrecision("eff_interest_rate"),
	collectability: smallint(),
	collectabilityGroup: smallint("collectability_group"),
	dpd: smallint(),
	dpdGroup: smallint("dpd_group"),
	dpdCounter: smallint("dpd_counter"),
	internalRatingCodeInitial: varchar("internal_rating_code_initial", { length: 5 }),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingIdInitial: smallint("ext_rating_id_initial"),
	extRatingCodeInitial: varchar("ext_rating_code_initial", { length: 5 }),
	extRatingAgencyInitial: varchar("ext_rating_agency_initial", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingAgency: varchar("ext_rating_agency", { length: 5 }),
	paymentCode: varchar("payment_code", { length: 20 }),
	paymentTerm: varchar("payment_term", { length: 2 }),
	paymentFreq: smallint("payment_freq"),
	intPmtTerm: varchar("int_pmt_term", { length: 2 }),
	intPmtFreq: smallint("int_pmt_freq"),
	amortizationType: varchar("amortization_type", { length: 5 }),
	pmtSchStatus: varchar("pmt_sch_status", { length: 5 }),
	eirSchStatus: varchar("eir_sch_status", { length: 5 }),
	repoFlag: boolean("repo_flag"),
	revolvingFlag: boolean("revolving_flag"),
	bmFlag: boolean("bm_flag"),
	committedFlag: boolean("committed_flag"),
	nplFlag: boolean("npl_flag"),
	nplDate: date("npl_date"),
	restructureFlag: boolean("restructure_flag"),
	restructureDate: date("restructure_date"),
	restructureReviewDate: date("restructure_review_date"),
	fibFlag: boolean("fib_flag"),
	fibStartDate: date("fib_start_date"),
	fibTenor: smallint("fib_tenor"),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }).default('0'),
	specialCaseFlag: boolean("special_case_flag"),
	impairedFlag: boolean("impaired_flag"),
	impairedStatus: char("impaired_status", { length: 1 }),
	segmentId: smallint("segment_id"),
	groupSegment: varchar("group_segment", { length: 50 }),
	segment: varchar({ length: 50 }),
	subSegment: varchar("sub_segment", { length: 50 }),
	eclModelId: smallint("ecl_model_id"),
	eadConfigId: smallint("ead_config_id"),
	pdConfigId: smallint("pd_config_id"),
	lgdConfigId: smallint("lgd_config_id"),
	sicrFlag: boolean("sicr_flag"),
	stageOriginal: varchar("stage_original", { length: 5 }),
	stageOverride: varchar("stage_override", { length: 5 }),
	stage: varchar({ length: 5 }),
	bucketIdOriginal: smallint("bucket_id_original"),
	bucketIdOverride: smallint("bucket_id_override"),
	bucketId: smallint("bucket_id"),
	bucketIdObligor: smallint("bucket_id_obligor"),
	bucketIdEd: smallint("bucket_id_ed"),
	interestBase: varchar("interest_base", { length: 2 }),
	sppiStatus: varchar("sppi_status", { length: 50 }),
	bmStatus: varchar("bm_status", { length: 50 }),
	assetClass: varchar("asset_class", { length: 50 }),
	currency: varchar({ length: 5 }),
	exchangeRate: doublePrecision("exchange_rate"),
	plafond: numeric({ precision: 32, scale: 6 }).default('0'),
	fullReleaseAmt: numeric("full_release_amt", { precision: 32, scale: 6 }).default('0'),
	unusedAmt: numeric("unused_amt", { precision: 32, scale: 6 }).default('0'),
	outstanding: numeric({ precision: 32, scale: 6 }).default('0'),
	outstandingWo: numeric("outstanding_wo", { precision: 32, scale: 6 }).default('0'),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }).default('0'),
	installmentAmt: numeric("installment_amt", { precision: 32, scale: 6 }).default('0'),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }).default('0'),
	fixInterestAmt: numeric("fix_interest_amt", { precision: 32, scale: 6 }).default('0'),
	initialFeeAmt: numeric("initial_fee_amt", { precision: 32, scale: 6 }).default('0'),
	unamortFeeAmt: numeric("unamort_fee_amt", { precision: 32, scale: 6 }).default('0'),
	amortFeeAmt: numeric("amort_fee_amt", { precision: 32, scale: 6 }).default('0'),
	initialCostAmt: numeric("initial_cost_amt", { precision: 32, scale: 6 }).default('0'),
	unamortCostAmt: numeric("unamort_cost_amt", { precision: 32, scale: 6 }).default('0'),
	amortCostAmt: numeric("amort_cost_amt", { precision: 32, scale: 6 }).default('0'),
	initialBmAmt: numeric("initial_bm_amt", { precision: 32, scale: 6 }).default('0'),
	unamortBmAmt: numeric("unamort_bm_amt", { precision: 32, scale: 6 }).default('0'),
	amortBmAmt: numeric("amort_bm_amt", { precision: 32, scale: 6 }).default('0'),
	initialGainlossAmt: numeric("initial_gainloss_amt", { precision: 32, scale: 6 }).default('0'),
	unamortGainlossAmt: numeric("unamort_gainloss_amt", { precision: 32, scale: 6 }).default('0'),
	amortGainlossAmt: numeric("amort_gainloss_amt", { precision: 32, scale: 6 }).default('0'),
	carryingAmt: numeric("carrying_amt", { precision: 32, scale: 6 }).default('0'),
	fairValueAmt: numeric("fair_value_amt", { precision: 32, scale: 6 }).default('0'),
	eclCaOnbsAmt: numeric("ecl_ca_onbs_amt", { precision: 32, scale: 6 }).default('0'),
	eclCaOffbsAmt: numeric("ecl_ca_offbs_amt", { precision: 32, scale: 6 }).default('0'),
	eclIaOnbsAmt: numeric("ecl_ia_onbs_amt", { precision: 32, scale: 6 }).default('0'),
	eclOverlayAmt: numeric("ecl_overlay_amt", { precision: 32, scale: 6 }).default('0'),
	eclFinalAmt: numeric("ecl_final_amt", { precision: 32, scale: 6 }).default('0'),
	unwindingCaAmt: numeric("unwinding_ca_amt", { precision: 32, scale: 6 }).default('0'),
	unwindingIaAmt: numeric("unwinding_ia_amt", { precision: 32, scale: 6 }).default('0'),
	unwindingIaSumAmt: numeric("unwinding_ia_sum_amt", { precision: 32, scale: 6 }).default('0'),
	eclBeginningBalance: numeric("ecl_beginning_balance", { precision: 32, scale: 6 }).default('0'),
	eclCharge: numeric("ecl_charge", { precision: 32, scale: 6 }).default('0'),
	eclWriteback: numeric("ecl_writeback", { precision: 32, scale: 6 }).default('0'),
	eclEndingBalance: numeric("ecl_ending_balance", { precision: 32, scale: 6 }).default('0'),
	lgd: doublePrecision(),
	weightedEffInterestRate: doublePrecision("weighted_eff_interest_rate"),
	weightedInterestRate: doublePrecision("weighted_interest_rate"),
});

export const frs9MasterAccountRepo = pgTable("frs9_master_account_repo", {
	prcDate: date("prc_date").notNull(),
	repoDate: date("repo_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	outstandingRepo: numeric("outstanding_repo", { precision: 32, scale: 6 }),
	eqvOsRepo: numeric("eqv_os_repo", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const frs9MasterAccountWo = pgTable("frs9_master_account_wo", {
	prcDate: date("prc_date"),
	woDate: date("wo_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	outstandingWo: numeric("outstanding_wo", { precision: 32, scale: 6 }),
	eqvOsWo: numeric("eqv_os_wo", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }).defaultNow(),
});

export const frs9MasterExchangeRate = pgTable("frs9_master_exchange_rate", {
	prcDate: date("prc_date"),
	currency: varchar({ length: 5 }),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }).defaultNow(),
});

export const frs9MasterTransactionCost = pgTable("frs9_master_transaction_cost", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	effectiveDate: date("effective_date"),
	dataSource: varchar("data_source", { length: 20 }),
	prdType: varchar("prd_type", { length: 20 }),
	prdCode: varchar("prd_code", { length: 20 }),
	branchCode: varchar("branch_code", { length: 20 }),
	debetCreditFlag: char("debet_credit_flag", { length: 1 }),
	currencyCode: varchar("currency_code", { length: 5 }),
	feeCostId: varchar("fee_cost_id", { length: 1 }).notNull(),
	trxCode: varchar("trx_code", { length: 20 }),
	orgCcyAmt: numeric("org_ccy_amt", { precision: 32, scale: 6 }),
	eqvLcyAmt: numeric("eqv_lcy_amt", { precision: 32, scale: 6 }),
});

export const frs9ParamBucketd = pgTable("frs9_param_bucketd", {
	pkid: smallserial().notNull(),
	pkidHeader: smallint("pkid_header"),
	bucketId: smallint("bucket_id"),
	bucketName: varchar("bucket_name", { length: 100 }),
	rangeStart: smallint("range_start"),
	rangeEnd: smallint("range_end"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamBucketh = pgTable("frs9_param_bucketh", {
	pkid: smallserial().notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketDesc: varchar("bucket_desc", { length: 255 }),
	basis: varchar({ length: 20 }),
	bucketDefault: smallint("bucket_default"),
	closedFlag: boolean("closed_flag").notNull(),
	woFlag: boolean("wo_flag").notNull(),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamCommond = pgTable("frs9_param_commond", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	paramCode: varchar("param_code", { length: 50 }).notNull(),
	paramSeq: integer("param_seq").notNull(),
	value1: varchar({ length: 100 }).notNull(),
	value2: varchar({ length: 100 }).notNull(),
	value3: varchar({ length: 50 }).notNull(),
	paramdesc: varchar({ length: 1000 }).notNull(),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamCommonh = pgTable("frs9_param_commonh", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	paramCode: varchar("param_code", { length: 10 }),
	paramName: varchar("param_name", { length: 255 }),
	paramUsage: varchar("param_usage", { length: 255 }),
	paramType: varchar("param_type", { length: 10 }),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
	bankingType: varchar("banking_type", { length: 20 }).default('conventional'),
	isActive: boolean("is_active").default(true),
	requiresApproval: boolean("requires_approval").default(false),
}, (table) => [
	check("chk_banking_type", sql`(banking_type)::text = ANY (ARRAY[('conventional'::character varying)::text, ('syariah'::character varying)::text, ('dual'::character varying)::text])`),
]);

export const frs9ParamJournal = pgTable("frs9_param_journal", {
	pkid: smallserial().notNull(),
	glGroup: varchar("gl_group", { length: 20 }),
	currency: char({ length: 3 }),
	glType: varchar("gl_type", { length: 20 }),
	glCode: varchar("gl_code", { length: 20 }),
	glNumber: varchar("gl_number", { length: 20 }),
	dbcr: char({ length: 1 }),
	glDesc: varchar("gl_desc", { length: 255 }),
	activeFlag: boolean("active_flag"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamProduct = pgTable("frs9_param_product", {
	pkid: smallserial().notNull(),
	dataSource: varchar("data_source", { length: 20 }).notNull(),
	prdGroup: varchar("prd_group", { length: 20 }).notNull(),
	prdType: varchar("prd_type", { length: 20 }).notNull(),
	prdCode: varchar("prd_code", { length: 20 }).notNull(),
	prdDesc: varchar("prd_desc", { length: 255 }).notNull(),
	currency: varchar({ length: 5 }).notNull(),
	amortizationType: varchar("amortization_type", { length: 10 }),
	alFlag: varchar("al_flag", { length: 1 }),
	impairedFlag: boolean("impaired_flag"),
	bmFlag: boolean("bm_flag"),
	expectedLife: integer("expected_life"),
	borrowingRate: doublePrecision("borrowing_rate"),
	marketRate: doublePrecision("market_rate"),
	activeFlag: boolean("active_flag").notNull(),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamScenarioRulesd = pgTable("frs9_param_scenario_rulesd", {
	pkid: smallserial().notNull(),
	ruleId: smallint("rule_id"),
	queryGroup: smallint("query_group"),
	seq: smallint(),
	tableName: varchar("table_name", { length: 30 }).notNull(),
	columnName: varchar("column_name", { length: 30 }).notNull(),
	dataType: varchar("data_type", { length: 15 }).notNull(),
	operator: varchar({ length: 10 }),
	value1: text().notNull(),
	value2: text(),
	condition: varchar({ length: 3 }),
	detailType: varchar("detail_type", { length: 50 }),
	stageFrom: varchar("stage_from", { length: 2 }),
	stageTo: varchar("stage_to", { length: 2 }),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamScenarioRulesh = pgTable("frs9_param_scenario_rulesh", {
	pkid: smallserial().notNull(),
	ruleName: varchar("rule_name", { length: 250 }).notNull(),
	ruleType: varchar("rule_type", { length: 50 }).notNull(),
	updatedTable: varchar("updated_table", { length: 30 }),
	updatedColumn: varchar("updated_column", { length: 30 }),
	value: varchar({ length: 250 }),
	seq: smallint(),
	activeFlag: boolean("active_flag"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamSegmentd = pgTable("frs9_param_segmentd", {
	pkid: smallserial().notNull(),
	segmentId: smallint("segment_id"),
	queryGroup: smallint("query_group"),
	seq: smallint(),
	tableName: varchar("table_name", { length: 30 }),
	columnName: varchar("column_name", { length: 30 }),
	dataType: varchar("data_type", { length: 15 }),
	operator: varchar({ length: 10 }),
	value1: text(),
	value2: text(),
	condition: varchar({ length: 3 }),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9ParamSegmenth = pgTable("frs9_param_segmenth", {
	pkid: smallserial().notNull(),
	groupSegment: varchar("group_segment", { length: 150 }),
	segment: varchar({ length: 150 }),
	subSegment: varchar("sub_segment", { length: 150 }),
	segmentType: varchar("segment_type", { length: 50 }),
	seq: smallint(),
	activeFlag: boolean("active_flag"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9PrcDate = pgTable("frs9_prc_date", {
	pkid: smallint().generatedAlwaysAsIdentity({ name: "frs9_prc_date_pkid_seq", startWith: 1, increment: 1, minValue: 1, maxValue: 32767, cache: 1 }),
	currdate: date().notNull(),
	prevdate: date(),
	batchStatus: varchar("batch_status", { length: 50 }),
	remark: varchar({ length: 250 }),
	lastProcessDate: date("last_process_date"),
	sessionid: varchar({ length: 50 }),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const frs9RModelSummary = pgTable("frs9_r_model_summary", {
	modelId: serial("model_id").notNull(),
	modelName: varchar("model_name", { length: 255 }),
	modelStatus: varchar("model_status", { length: 50 }),
	dependentVariable: varchar("dependent_variable", { length: 100 }),
	rSquared: numeric("r_squared", { precision: 10, scale: 4 }),
	mape: numeric({ precision: 10, scale: 4 }),
	createdDate: timestamp("created_date", { mode: 'string' }).default(sql`CURRENT_TIMESTAMP`),
	dataFile: varchar("data_file", { length: 255 }),
});

export const frs9Statistic = pgTable("frs9_statistic", {
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pkid: bigint({ mode: "number" }).generatedAlwaysAsIdentity({ name: "frs9_statistic_pkid_seq", startWith: 1, increment: 1, minValue: 1, maxValue: 9223372036854775807, cache: 1 }),
	prcDate: date("prc_date"),
	spName: varchar("sp_name", { length: 100 }),
	startDate: timestamp("start_date", { mode: 'string' }),
	endDate: timestamp("end_date", { mode: 'string' }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	counter: bigint({ mode: "number" }),
	sessionid: varchar({ length: 100 }),
	iscomplete: varchar({ length: 2 }),
	prcName: varchar("prc_name", { length: 100 }),
	prcProcessTime: varchar("prc_process_time", { length: 30 }),
	sessionProcessTime: varchar("session_process_time", { length: 30 }),
	remark: varchar({ length: 250 }),
	parameter: varchar({ length: 255 }),
});

export const stgFrs9MasterAccountBpf = pgTable("stg_frs9_master_account_bpf", {
	prcDate: date("prc_date"),
	accountNumber: varchar("account_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	cifName: varchar("cif_name", { length: 150 }),
	accountStatus: varchar("account_status", { length: 20 }),
	dataSource: varchar("data_source", { length: 20 }),
	prdGroup: varchar("prd_group", { length: 20 }),
	prdType: varchar("prd_type", { length: 50 }),
	prdCode: varchar("prd_code", { length: 20 }),
	branchCode: varchar("branch_code", { length: 50 }),
	tenorOrg: smallint("tenor_org"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	paidOffDate: date("paid_off_date"),
	writeOffDate: date("write_off_date"),
	firstPaymentDate: date("first_payment_date"),
	nextPaymentDate: date("next_payment_date"),
	lastPaymentDate: date("last_payment_date"),
	graceType: varchar("grace_type", { length: 5 }),
	graceStartDate: date("grace_start_date"),
	graceEndDate: date("grace_end_date"),
	interestRate: doublePrecision("interest_rate"),
	effInterestRate: doublePrecision("eff_interest_rate"),
	collectability: smallint(),
	dpd: integer(),
	extRatingCodeInitial: varchar("ext_rating_code_initial", { length: 5 }),
	extRatingAgencyInitial: varchar("ext_rating_agency_initial", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingAgency: varchar("ext_rating_agency", { length: 5 }),
	paymentCode: varchar("payment_code", { length: 20 }),
	paymentTerm: varchar("payment_term", { length: 2 }),
	paymentFreq: smallint("payment_freq"),
	intPmtTerm: varchar("int_pmt_term", { length: 2 }),
	intPmtFreq: smallint("int_pmt_freq"),
	nplFlag: boolean("npl_flag"),
	nplDate: date("npl_date"),
	restructureFlag: boolean("restructure_flag"),
	restructureDate: date("restructure_date"),
	restructureReviewDate: date("restructure_review_date"),
	groupSegment: varchar("group_segment", { length: 50 }),
	segment: varchar({ length: 50 }),
	subSegment: varchar("sub_segment", { length: 50 }),
	interestBase: varchar("interest_base", { length: 2 }),
	assetClass: varchar("asset_class", { length: 50 }),
	currency: varchar({ length: 5 }),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	unusedAmt: numeric("unused_amt", { precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	outstandingWo: numeric("outstanding_wo", { precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	installmentAmt: numeric("installment_amt", { precision: 32, scale: 6 }),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }),
	fixInterestAmt: numeric("fix_interest_amt", { precision: 32, scale: 6 }),
});

export const stgMasterLoan = pgTable("stg_master_loan", {
	downloadDate: timestamp("download_date", { mode: 'string' }),
	accountNoCore: varchar("account_no_core", { length: 255 }),
	accountNo: varchar("account_no", { length: 255 }),
	segment: varchar({ length: 255 }),
	bucketOri: doublePrecision("bucket_ori"),
	bucketCap: doublePrecision("bucket_cap"),
	bucketPreRest: doublePrecision("bucket_pre_rest"),
	bucketFinal: doublePrecision("bucket_final"),
	stage: doublePrecision(),
	startDate: timestamp("start_date", { mode: 'string' }),
	maturityDate: timestamp("maturity_date", { mode: 'string' }),
	adjustedMaturityDate: timestamp("adjusted_maturity_date", { mode: 'string' }),
	graceStartDate: timestamp("grace_start_date", { mode: 'string' }),
	graceEndDate: timestamp("grace_end_date", { mode: 'string' }),
	graceFlag: varchar("grace_flag", { length: 255 }),
	gracePeriodMonths: doublePrecision("grace_period_months"),
	watchlistFlag: varchar("watchlist_flag", { length: 255 }),
	restruFlag: varchar("restru_flag", { length: 255 }),
	restructureDate: timestamp("restructure_date", { mode: 'string' }),
	nextRestruInstalmentDate: timestamp("next_restru_instalment_date", { mode: 'string' }),
	osBalance: doublePrecision("os_balance"),
	osPrincipal: doublePrecision("os_principal"),
	instalmentAmount: doublePrecision("instalment_amount"),
	effectiveRatePercent: doublePrecision("effective_rate_percent"),
	lgdPercent: doublePrecision("lgd_percent"),
	tenor: doublePrecision(),
	remainingTenor: doublePrecision("remaining_tenor"),
	undrawn: doublePrecision(),
	assetType: varchar("asset_type", { length: 255 }),
	assetCondition: varchar("asset_condition", { length: 255 }),
	branch: varchar({ length: 255 }),
});

export const stgPaymSchd = pgTable("stg_paym_schd", {
	prcDate: date("prc_date").notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	idays: smallint(),
	paymentDate: date("payment_date"),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	installment: numeric({ precision: 33, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tbluMasterCollateral = pgTable("tblu_master_collateral", {
	pkid: smallserial().notNull(),
	prcDate: date("prc_date").notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	collateralDescription: varchar("collateral_description", { length: 100 }),
	collateralValue: numeric("collateral_value", { precision: 32, scale: 6 }).default('0'),
	sellingDate: date("selling_date"),
	sellingPrice: numeric("selling_price", { precision: 32, scale: 6 }),
	workoutPeriod: smallint("workout_period"),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const tbluMasterPdProxy = pgTable("tblu_master_pd_proxy", {
	pkid: smallserial().notNull(),
	prcDate: date("prc_date").notNull(),
	ratingAgency: varchar("rating_agency", { length: 20 }).notNull(),
	ratingFrom: varchar("rating_from", { length: 10 }).notNull(),
	ratingTo: varchar("rating_to", { length: 10 }).notNull(),
	pdRate: doublePrecision("pd_rate").notNull(),
	createdby: varchar({ length: 50 }).notNull(),
	createddate: timestamp({ mode: 'string' }).notNull(),
	createdhost: varchar({ length: 50 }).notNull(),
	updatedby: varchar({ length: 50 }),
	updateddate: timestamp({ mode: 'string' }),
	updatedhost: varchar({ length: 50 }),
});

export const tbluPaymSchd = pgTable("tblu_paym_schd", {
	prcDate: date("prc_date").notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	idays: smallint(),
	paymentDate: date("payment_date"),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
	installment: numeric({ precision: 33, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFmaPreview = pgTable("tmp_fma_preview", {
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pkid: bigint({ mode: "number" }).notNull(),
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 50 }).notNull(),
	facilityNumber: varchar("facility_number", { length: 50 }),
	cifType: varchar("cif_type", { length: 50 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	cifName: varchar("cif_name", { length: 150 }),
	accountStatus: varchar("account_status", { length: 20 }),
	dataSource: varchar("data_source", { length: 20 }),
	prdGroup: varchar("prd_group", { length: 20 }),
	prdType: varchar("prd_type", { length: 50 }),
	prdCode: varchar("prd_code", { length: 20 }),
	glGroup: varchar("gl_group", { length: 50 }),
	branchCode: varchar("branch_code", { length: 50 }),
	tenorOrg: smallint("tenor_org"),
	remainingTenor: smallint("remaining_tenor"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	startAmortDate: date("start_amort_date"),
	endAmortDate: date("end_amort_date"),
	paidOffDate: date("paid_off_date"),
	writeOffDate: date("write_off_date"),
	firstPaymentDate: date("first_payment_date"),
	nextPaymentDate: date("next_payment_date"),
	lastPaymentDate: date("last_payment_date"),
	nextSchPrinDate: date("next_sch_prin_date"),
	nextSchIntDate: date("next_sch_int_date"),
	graceType: varchar("grace_type", { length: 5 }),
	graceStartDate: date("grace_start_date"),
	graceEndDate: date("grace_end_date"),
	marketRate: doublePrecision("market_rate"),
	interestRate: doublePrecision("interest_rate"),
	effInterestRate: doublePrecision("eff_interest_rate"),
	collectability: smallint(),
	collectabilityGroup: smallint("collectability_group"),
	dpd: smallint(),
	dpdGroup: smallint("dpd_group"),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingAgency: varchar("ext_rating_agency", { length: 5 }),
	paymentCode: varchar("payment_code", { length: 20 }),
	paymentTerm: varchar("payment_term", { length: 2 }),
	paymentFreq: smallint("payment_freq"),
	intPmtTerm: varchar("int_pmt_term", { length: 2 }),
	intPmtFreq: smallint("int_pmt_freq"),
	pmtSchStatus: char("pmt_sch_status", { length: 1 }),
	eirSchStatus: char("eir_sch_status", { length: 1 }),
	revolvingFlag: boolean("revolving_flag"),
	bmFlag: boolean("bm_flag"),
	committedFlag: boolean("committed_flag"),
	nplFlag: boolean("npl_flag"),
	nplDate: date("npl_date"),
	restructureFlag: boolean("restructure_flag"),
	restructureDate: date("restructure_date"),
	restructureReviewDate: date("restructure_review_date"),
	repoFlag: boolean("repo_flag"),
	fibFlag: boolean("fib_flag"),
	fibStartDate: date("fib_start_date"),
	fibTenor: smallint("fib_tenor"),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }).default('0'),
	specialCaseFlag: boolean("special_case_flag"),
	impairedFlag: boolean("impaired_flag"),
	impairedStatus: char("impaired_status", { length: 1 }),
	segmentId: smallint("segment_id"),
	groupSegment: varchar("group_segment", { length: 50 }),
	segment: varchar({ length: 50 }),
	subSegment: varchar("sub_segment", { length: 50 }),
	sicrFlag: boolean("sicr_flag"),
	stage: varchar({ length: 5 }),
	bucketId: smallint("bucket_id"),
	bucketIdObligor: smallint("bucket_id_obligor"),
	bucketIdEd: smallint("bucket_id_ed"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eclModelId: bigint("ecl_model_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadConfigId: bigint("ead_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	lgd: doublePrecision().default(0),
	interestBase: varchar("interest_base", { length: 2 }),
	sppiStatus: varchar("sppi_status", { length: 50 }),
	bmStatus: varchar("bm_status", { length: 50 }),
	assetClass: varchar("asset_class", { length: 50 }),
	currency: varchar({ length: 5 }),
	exchangeRate: doublePrecision("exchange_rate"),
	plafond: numeric({ precision: 32, scale: 6 }).default('0'),
	fullReleaseAmt: numeric("full_release_amt", { precision: 32, scale: 6 }).default('0'),
	unusedAmt: numeric("unused_amt", { precision: 32, scale: 6 }).default('0'),
	outstanding: numeric({ precision: 32, scale: 6 }).default('0'),
	outstandingWo: numeric("outstanding_wo", { precision: 32, scale: 6 }).default('0'),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }).default('0'),
	installmentAmt: numeric("installment_amt", { precision: 32, scale: 6 }).default('0'),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }).default('0'),
	fixInterestAmt: numeric("fix_interest_amt", { precision: 32, scale: 6 }).default('0'),
	initialFeeAmt: numeric("initial_fee_amt", { precision: 32, scale: 6 }).default('0'),
	unamortFeeAmt: numeric("unamort_fee_amt", { precision: 32, scale: 6 }).default('0'),
	amortFeeAmt: numeric("amort_fee_amt", { precision: 32, scale: 6 }).default('0'),
	initialCostAmt: numeric("initial_cost_amt", { precision: 32, scale: 6 }).default('0'),
	unamortCostAmt: numeric("unamort_cost_amt", { precision: 32, scale: 6 }).default('0'),
	amortCostAmt: numeric("amort_cost_amt", { precision: 32, scale: 6 }).default('0'),
	initialBmAmt: numeric("initial_bm_amt", { precision: 32, scale: 6 }).default('0'),
	unamortBmAmt: numeric("unamort_bm_amt", { precision: 32, scale: 6 }).default('0'),
	amortBmAmt: numeric("amort_bm_amt", { precision: 32, scale: 6 }).default('0'),
	carryingAmt: numeric("carrying_amt", { precision: 32, scale: 6 }).default('0'),
	eclCaOnbsAmt: numeric("ecl_ca_onbs_amt", { precision: 32, scale: 6 }).default('0'),
	eclCaOffbsAmt: numeric("ecl_ca_offbs_amt", { precision: 32, scale: 6 }).default('0'),
	eclIaOnbsAmt: numeric("ecl_ia_onbs_amt", { precision: 32, scale: 6 }).default('0'),
	eclFinalAmt: numeric("ecl_final_amt", { precision: 32, scale: 6 }).default('0'),
	unwindingCaAmt: numeric("unwinding_ca_amt", { precision: 32, scale: 6 }).default('0'),
	unwindingIaAmt: numeric("unwinding_ia_amt", { precision: 32, scale: 6 }).default('0'),
	unwindingIaSumAmt: numeric("unwinding_ia_sum_amt", { precision: 32, scale: 6 }),
	eclBeginningBalance: numeric("ecl_beginning_balance", { precision: 32, scale: 6 }).default('0'),
	eclCharge: numeric("ecl_charge", { precision: 32, scale: 6 }).default('0'),
	eclWriteback: numeric("ecl_writeback", { precision: 32, scale: 6 }).default('0'),
	eclEndingBalance: numeric("ecl_ending_balance", { precision: 32, scale: 6 }).default('0'),
	dpdCounter: smallint("dpd_counter"),
	internalRatingCodeInitial: varchar("internal_rating_code_initial", { length: 5 }),
	extRatingIdInitial: smallint("ext_rating_id_initial"),
	extRatingCodeInitial: varchar("ext_rating_code_initial", { length: 5 }),
	extRatingAgencyInitial: varchar("ext_rating_agency_initial", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	amortizationType: varchar("amortization_type", { length: 5 }),
	stageOriginal: varchar("stage_original", { length: 5 }),
	stageOverride: varchar("stage_override", { length: 5 }),
	bucketIdOriginal: smallint("bucket_id_original"),
	bucketIdOverride: smallint("bucket_id_override"),
	initialGainlossAmt: numeric("initial_gainloss_amt", { precision: 32, scale: 6 }),
	unamortGainlossAmt: numeric("unamort_gainloss_amt", { precision: 32, scale: 6 }),
	amortGainlossAmt: numeric("amort_gainloss_amt", { precision: 32, scale: 6 }),
	fairValueAmt: numeric("fair_value_amt", { precision: 32, scale: 6 }),
	eclOverlayAmt: numeric("ecl_overlay_amt", { precision: 32, scale: 6 }),
	weightedEffInterestRate: doublePrecision("weighted_eff_interest_rate"),
	weightedInterestRate: doublePrecision("weighted_interest_rate"),
});

export const tmpFrs9EclFma = pgTable("tmp_frs9_ecl_fma", {
	pkid: bigserial({ mode: "bigint" }).notNull(),
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountNumber: varchar("account_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 100 }),
	cifNumber: varchar("cif_number", { length: 100 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	remainingTenor: smallint("remaining_tenor"),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	nextPaymentDate: date("next_payment_date"),
	nextSchPrinDate: date("next_sch_prin_date"),
	nextSchIntDate: date("next_sch_int_date"),
	graceType: varchar("grace_type", { length: 5 }),
	graceStartDate: date("grace_start_date"),
	graceEndDate: date("grace_end_date"),
	fibFlag: boolean("fib_flag"),
	fibStartDate: date("fib_start_date"),
	fibTenor: smallint("fib_tenor"),
	paymentCode: varchar("payment_code", { length: 20 }),
	paymentTerm: varchar("payment_term", { length: 2 }),
	paymentFreq: integer("payment_freq"),
	intPmtTerm: varchar("int_pmt_term", { length: 2 }),
	intPmtFreq: integer("int_pmt_freq"),
	defaultFlag: boolean("default_flag"),
	dpd: integer(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eclModelId: bigint("ecl_model_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadConfigId: bigint("ead_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadMethod: bigint("ead_method", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	eadCalcMethod: bigint("ead_calc_method", { mode: "number" }),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketId: integer("bucket_id"),
	currency: varchar({ length: 5 }),
	stage: smallint(),
	interestBase: varchar("interest_base", { length: 2 }),
	interestRate: doublePrecision("interest_rate"),
	eir: doublePrecision(),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	installmentAmt: numeric("installment_amt", { precision: 32, scale: 6 }),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }),
	fixInterestAmt: numeric("fix_interest_amt", { precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	plafond: numeric({ precision: 32, scale: 6 }),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	accruedInterest: numeric("accrued_interest", { precision: 32, scale: 6 }),
	unamortCostAmt: numeric("unamort_cost_amt", { precision: 32, scale: 6 }),
	unamortFeeAmt: numeric("unamort_fee_amt", { precision: 32, scale: 6 }),
	eadBalance: numeric("ead_balance", { precision: 32, scale: 6 }),
	beginningBalance: numeric("beginning_balance", { precision: 32, scale: 6 }),
	eclAmount: numeric("ecl_amount", { precision: 32, scale: 6 }),
	iaUnwindingAmount: numeric("ia_unwinding_amount", { precision: 32, scale: 6 }),
	iaUnwindingSumAmount: numeric("ia_unwinding_sum_amount", { precision: 32, scale: 6 }),
	writebackAmount: numeric("writeback_amount", { precision: 32, scale: 6 }),
	chargeAmount: numeric("charge_amount", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9ImpCaEadPaymAvg = pgTable("tmp_frs9_imp_ca_ead_paym_avg", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	tenor: smallint(),
	counter: smallint(),
	paymAvg: doublePrecision("paym_avg"),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9ImpCaLgdRecD = pgTable("tmp_frs9_imp_ca_lgd_rec_d", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdMethod: bigint("lgd_method", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	eqvOs: numeric("eqv_os", { precision: 32, scale: 6 }),
	eir: doublePrecision(),
	seq: smallint(),
	eqvRec: numeric("eqv_rec", { precision: 32, scale: 6 }),
	npvEqvRec: numeric("npv_eqv_rec", { precision: 32, scale: 6 }),
	createdby: varchar({ length: 100 }),
	createddate: timestamp({ mode: 'string' }),
	rn: integer(),
});

export const tmpFrs9ImpCaPdData = pgTable("tmp_frs9_imp_ca_pd_data", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	pdMethod: smallint("pd_method"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountStatus: char("account_status", { length: 1 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	currency: varchar({ length: 5 }),
	bucketDefault: smallint("bucket_default"),
	includeClosedFlag: boolean("include_closed_flag"),
	includeWoFlag: boolean("include_wo_flag"),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketId: smallint("bucket_id"),
	dpd: smallint(),
	collectability: smallint(),
	internalRatingCode: varchar("internal_rating_code", { length: 5 }),
	extRatingCode: varchar("ext_rating_code", { length: 5 }),
	extRatingId: smallint("ext_rating_id"),
	defaultFlag: boolean("default_flag"),
	woFlag: boolean("wo_flag"),
	remainingTenor: smallint("remaining_tenor"),
	plafond: numeric({ precision: 32, scale: 6 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	createdby: varchar({ length: 50 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9ImpCaPdScnCurr = pgTable("tmp_frs9_imp_ca_pd_scn_curr", {
	period: date().notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }).notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountStatus: char("account_status", { length: 1 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	bucketId: smallint("bucket_id"),
	calcAmount: numeric("calc_amount", { precision: 32, scale: 6 }),
	woFlag: boolean("wo_flag"),
	includeWoFlag: boolean("include_wo_flag"),
	includeClosedFlag: boolean("include_closed_flag"),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9ImpCaPdScnPrev = pgTable("tmp_frs9_imp_ca_pd_scn_prev", {
	period: date().notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }).notNull(),
	bucketGroup: varchar("bucket_group", { length: 30 }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountStatus: char("account_status", { length: 1 }),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	bucketId: smallint("bucket_id"),
	calcAmount: numeric("calc_amount", { precision: 32, scale: 6 }),
	woFlag: boolean("wo_flag"),
	includeWoFlag: boolean("include_wo_flag"),
	includeClosedFlag: boolean("include_closed_flag"),
	createdby: varchar({ length: 36 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9ImpCaPdStructure = pgTable("tmp_frs9_imp_ca_pd_structure", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	pdMethod: smallint("pd_method"),
	bucketGroup: varchar("bucket_group", { length: 100 }),
	bucketId: integer("bucket_id"),
	flSeq: integer("fl_seq"),
	flYear: integer("fl_year"),
	flMonth: integer("fl_month"),
	pd: doublePrecision(),
});

export const tmpFrs9ImpCaResultEad = pgTable("tmp_frs9_imp_ca_result_ead", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	flSeq: smallint("fl_seq"),
	paymAvg: doublePrecision("paym_avg"),
	principal: numeric({ precision: 32, scale: 6 }),
	sumPrincipal: numeric("sum_principal", { precision: 32, scale: 6 }),
	nextInterest: numeric("next_interest", { precision: 32, scale: 6 }),
	sumNextInterest: numeric("sum_next_interest", { precision: 32, scale: 6 }),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9ImpCaResultLgd = pgTable("tmp_frs9_imp_ca_result_lgd", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	lgdConfigId: bigint("lgd_config_id", { mode: "number" }),
	lgd: doublePrecision(),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9ImpCaResultPd = pgTable("tmp_frs9_imp_ca_result_pd", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	segmentId: bigint("segment_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	pdConfigId: bigint("pd_config_id", { mode: "number" }),
	scenarioNo: smallint("scenario_no"),
	bucketId: smallint("bucket_id"),
	flSeq: smallint("fl_seq"),
	pd: doublePrecision(),
	createddate: timestamp({ mode: 'string' }),
});

export const tmpFrs9MasterAccountPrev = pgTable("tmp_frs9_master_account_prev", {
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	cifNumber: varchar("cif_number", { length: 50 }),
	facilityNumber: varchar("facility_number", { length: 50 }),
	branchCode: varchar("branch_code", { length: 50 }),
	interestRate: doublePrecision("interest_rate"),
	effInterestRate: doublePrecision("eff_interest_rate"),
	maturityDate: date("maturity_date"),
	installmentAmt: numeric("installment_amt", { precision: 32, scale: 6 }),
	segmentId: smallint("segment_id"),
	stage: varchar({ length: 5 }),
	impairedStatus: char("impaired_status", { length: 1 }),
	outstanding: numeric({ precision: 32, scale: 6 }),
	currency: varchar({ length: 5 }),
	exchangeRate: numeric("exchange_rate", { precision: 32, scale: 6 }),
	eclBeginningBalance: numeric("ecl_beginning_balance", { precision: 32, scale: 6 }),
	eclCaOnbsAmt: numeric("ecl_ca_onbs_amt", { precision: 32, scale: 6 }),
	eclCaOffbsAmt: numeric("ecl_ca_offbs_amt", { precision: 32, scale: 6 }),
	eclIaOnbsAmt: numeric("ecl_ia_onbs_amt", { precision: 32, scale: 6 }),
	eclFinalAmt: numeric("ecl_final_amt", { precision: 32, scale: 6 }),
	unwindingCaAmt: numeric("unwinding_ca_amt", { precision: 32, scale: 6 }),
	unwindingIaAmt: numeric("unwinding_ia_amt", { precision: 32, scale: 6 }),
	unwindingIaSumAmt: numeric("unwinding_ia_sum_amt", { precision: 32, scale: 6 }),
	eclCharge: numeric("ecl_charge", { precision: 32, scale: 6 }),
	eclWriteback: numeric("ecl_writeback", { precision: 32, scale: 6 }),
});

export const tmpImpCaEadCurr = pgTable("tmp_imp_ca_ead_curr", {
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 100 }).notNull(),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	paymentStartDate: date("payment_start_date"),
	stage: char({ length: 1 }),
	remainingTenor: integer("remaining_tenor"),
	interestRate: doublePrecision("interest_rate"),
	paymentCode: varchar("payment_code", { length: 20 }),
	interestBase: varchar("interest_base", { length: 2 }),
	nextEomPaymentDate: date("next_eom_payment_date"),
	nextPaymentDate: date("next_payment_date"),
	paymentTerm: char("payment_term", { length: 1 }),
	paymentFreq: integer("payment_freq"),
	nextSchPrinDate: date("next_sch_prin_date"),
	intPmtTerm: char("int_pmt_term", { length: 1 }),
	intPmtFreq: integer("int_pmt_freq"),
	nextSchIntDate: date("next_sch_int_date"),
	graceType: varchar("grace_type", { length: 1 }),
	graceStartDate: date("grace_start_date"),
	graceEndDate: date("grace_end_date"),
	fibFlag: boolean("fib_flag"),
	fibStartDate: date("fib_start_date"),
	fibTenor: integer("fib_tenor"),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }),
	fixInterestAmt: numeric("fix_interest_amt", { precision: 32, scale: 6 }),
	mob: smallint().notNull(),
	idays: integer(),
	eomPaymentDate: date("eom_payment_date"),
	paymentDate: date("payment_date"),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	installment: numeric({ precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
});

export const tmpImpCaEadPrev = pgTable("tmp_imp_ca_ead_prev", {
	prcDate: date("prc_date").notNull(),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }).notNull(),
	accountNumber: varchar("account_number", { length: 100 }).notNull(),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	paymentStartDate: date("payment_start_date"),
	stage: char({ length: 1 }),
	remainingTenor: integer("remaining_tenor"),
	interestRate: doublePrecision("interest_rate"),
	paymentCode: varchar("payment_code", { length: 20 }),
	interestBase: varchar("interest_base", { length: 2 }),
	nextEomPaymentDate: date("next_eom_payment_date"),
	nextPaymentDate: date("next_payment_date"),
	paymentTerm: char("payment_term", { length: 1 }),
	paymentFreq: integer("payment_freq"),
	nextSchPrinDate: date("next_sch_prin_date"),
	intPmtTerm: char("int_pmt_term", { length: 1 }),
	intPmtFreq: integer("int_pmt_freq"),
	nextSchIntDate: date("next_sch_int_date"),
	graceType: varchar("grace_type", { length: 1 }),
	graceStartDate: date("grace_start_date"),
	graceEndDate: date("grace_end_date"),
	fibFlag: boolean("fib_flag"),
	fibStartDate: date("fib_start_date"),
	fibTenor: integer("fib_tenor"),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }),
	fixInterestAmt: numeric("fix_interest_amt", { precision: 32, scale: 6 }),
	mob: smallint().notNull(),
	idays: integer(),
	eomPaymentDate: date("eom_payment_date"),
	paymentDate: date("payment_date"),
	osBalance: numeric("os_balance", { precision: 32, scale: 6 }),
	installment: numeric({ precision: 32, scale: 6 }),
	principal: numeric({ precision: 32, scale: 6 }),
	interest: numeric({ precision: 32, scale: 6 }),
});

export const uploadHistory = pgTable("upload_history", {
	id: serial().notNull(),
	filename: varchar({ length: 255 }),
	fileType: varchar("file_type", { length: 50 }),
	purpose: varchar({ length: 100 }),
	rows: integer(),
	columns: integer(),
	uploadTime: timestamp("upload_time", { mode: 'string' }).default(sql`CURRENT_TIMESTAMP`),
	data: text(),
});
export const viewImpCaEclConfig = pgView("view_imp_ca_ecl_config", {
	eclModelId: smallint("ecl_model_id"),
	effectiveDate: date("effective_date"),
	module: varchar({ length: 10 }),
	activeFlag: boolean("active_flag"),
	stageRuleId: smallint("stage_rule_id"),
	pfSegmentId: smallint("pf_segment_id"),
	pdModelId: smallint("pd_model_id"),
	lgdModelId: smallint("lgd_model_id"),
	eadModelId: smallint("ead_model_id"),
	overlayRate: smallint("overlay_rate"),
	periodType: smallint("period_type"),
	periodDate: date("period_date"),
	createdby: varchar({ length: 50 }),
}).as(sql`SELECT h.pkid AS ecl_model_id, h.effective_date, h.module, h.active_flag, d.stage_rule_id, d.pf_segment_id, d.pd_model_id, d.lgd_model_id, d.ead_model_id, d.overlay_rate, d.period_type, d.period_date, d.createdby FROM frs9_imp_ca_ecl_configh h JOIN frs9_imp_ca_ecl_configd d ON h.pkid = d.ecl_model_id`);

export const viewParamScenarioRules = pgView("view_param_scenario_rules", {
	ruleId: smallint("rule_id"),
	ruleName: varchar("rule_name", { length: 250 }),
	ruleType: varchar("rule_type", { length: 50 }),
	updatedTable: varchar("updated_table", { length: 30 }),
	updatedColumn: varchar("updated_column", { length: 30 }),
	hSeq: smallint("h_seq"),
	value: varchar({ length: 250 }),
	activeFlag: boolean("active_flag"),
	queryGroup: smallint("query_group"),
	dSeq: smallint("d_seq"),
	tableName: varchar("table_name", { length: 30 }),
	columnName: varchar("column_name", { length: 30 }),
	dataType: varchar("data_type", { length: 15 }),
	operator: varchar({ length: 10 }),
	value1: text(),
	value2: text(),
	condition: varchar({ length: 3 }),
	detailType: varchar("detail_type", { length: 50 }),
	stageFrom: varchar("stage_from", { length: 2 }),
	stageTo: varchar("stage_to", { length: 2 }),
}).as(sql`SELECT h.pkid AS rule_id, h.rule_name, h.rule_type, h.updated_table, h.updated_column, h.seq AS h_seq, h.value, h.active_flag, d.query_group, d.seq AS d_seq, d.table_name, d.column_name, d.data_type, d.operator, d.value1, d.value2, d.condition, d.detail_type, d.stage_from, d.stage_to FROM frs9_param_scenario_rulesh h JOIN frs9_param_scenario_rulesd d ON h.pkid = d.rule_id`);

export const viewParamSegmentRules = pgView("view_param_segment_rules", {
	segmentId: smallint("segment_id"),
	groupSegment: varchar("group_segment", { length: 150 }),
	segment: varchar({ length: 150 }),
	subSegment: varchar("sub_segment", { length: 150 }),
	segmentType: varchar("segment_type", { length: 50 }),
	hSeq: smallint("h_seq"),
	activeFlag: boolean("active_flag"),
	queryGroup: smallint("query_group"),
	dSeq: smallint("d_seq"),
	tableName: varchar("table_name", { length: 30 }),
	columnName: varchar("column_name", { length: 30 }),
	dataType: varchar("data_type", { length: 15 }),
	operator: varchar({ length: 10 }),
	value1: text(),
	value2: text(),
	condition: varchar({ length: 3 }),
}).as(sql`SELECT d.segment_id, h.group_segment, h.segment, h.sub_segment, h.segment_type, h.seq AS h_seq, h.active_flag, d.query_group, d.seq AS d_seq, d.table_name, d.column_name, d.data_type, d.operator, d.value1, d.value2, d.condition FROM frs9_param_segmenth h JOIN frs9_param_segmentd d ON h.pkid = d.segment_id`);

export const vwFrs9FirstDefault = pgView("vw_frs9_first_default", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	ruleId: bigint("rule_id", { mode: "number" }),
	osAtDefault: numeric("os_at_default", { precision: 32, scale: 6 }),
	eqvAtDefault: numeric("eqv_at_default", { precision: 32, scale: 6 }),
	eirAtDefault: doublePrecision("eir_at_default"),
}).as(sql`WITH cte_first_default AS ( SELECT frs9_default.prc_date, frs9_default.account_id, frs9_default.rule_id, frs9_default.os_at_default, frs9_default.eqv_at_default, frs9_default.eir_at_default, row_number() OVER (PARTITION BY frs9_default.account_id, frs9_default.rule_id ORDER BY frs9_default.prc_date) AS rn FROM frs9_default ) SELECT prc_date, account_id, rule_id, os_at_default, eqv_at_default, eir_at_default FROM cte_first_default WHERE rn = 1`);

export const vwFrs9ImpCaEadProcessEadCurr = pgView("vw_frs9_imp_ca_ead_process_ead_curr", {
	prcDate: date("prc_date"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	accountNumber: varchar("account_number", { length: 100 }),
	startDate: date("start_date"),
	maturityDate: date("maturity_date"),
	paymentStartDate: date("payment_start_date"),
	stage: char({ length: 1 }),
	remainingTenor: integer("remaining_tenor"),
	interestRate: doublePrecision("interest_rate"),
	paymentCode: varchar("payment_code", { length: 20 }),
	interestBase: varchar("interest_base", { length: 2 }),
	paymentTerm: char("payment_term", { length: 1 }),
	paymentFreq: integer("payment_freq"),
	nextSchPrinDate: date("next_sch_prin_date"),
	intPmtTerm: char("int_pmt_term", { length: 1 }),
	intPmtFreq: integer("int_pmt_freq"),
	nextSchIntDate: date("next_sch_int_date"),
	graceType: varchar("grace_type", { length: 1 }),
	graceStartDate: date("grace_start_date"),
	graceEndDate: date("grace_end_date"),
	fibFlag: boolean("fib_flag"),
	fibStartDate: date("fib_start_date"),
	fibTenor: integer("fib_tenor"),
	fibAmt: numeric("fib_amt", { precision: 32, scale: 6 }),
	fixPrincipalAmt: numeric("fix_principal_amt", { precision: 32, scale: 6 }),
	fixInterestAmt: numeric("fix_interest_amt", { precision: 32, scale: 6 }),
	idays: integer(),
	eomPaymentDate: date("eom_payment_date"),
	paymentDate: date("payment_date"),
	osBalance: numeric("os_balance"),
	installment: numeric({ precision: 32, scale: 6 }),
	principal: numeric(),
	interest: numeric(),
}).as(sql`SELECT prc_date, account_id, account_number, start_date, maturity_date, payment_start_date, stage, remaining_tenor, interest_rate, payment_code, interest_base, payment_term, payment_freq, next_sch_prin_date, int_pmt_term, int_pmt_freq, next_sch_int_date, grace_type, grace_start_date, grace_end_date, fib_flag, fib_start_date, fib_tenor, fib_amt, fix_principal_amt, fix_interest_amt, next_payment_date - payment_date AS idays, next_eom_payment_date AS eom_payment_date, next_payment_date AS payment_date, os_balance - fn_frs9_imp_ca_ead_process_principal_amt(next_payment_date, maturity_date, os_balance, grace_type::text, grace_end_date, payment_code::text, interest_base::text, interest_rate, installment, fix_principal_amt) AS os_balance, installment, fn_frs9_imp_ca_ead_process_principal_amt(next_payment_date, maturity_date, os_balance, grace_type::text, grace_end_date, payment_code::text, interest_base::text, interest_rate, installment, fix_principal_amt) AS principal, fn_frs9_imp_ca_ead_process_interest_amt(grace_type::text, next_payment_date, grace_end_date, payment_code::text, interest_base::text, os_balance, interest_rate, fix_interest_amt) AS interest FROM tmp_imp_ca_ead_curr m WHERE payment_date <= maturity_date AND os_balance > 0::numeric`);

export const vwFrs9MaxBucket = pgView("vw_frs9_max_bucket", {
	bucketGroup: varchar("bucket_group", { length: 30 }),
	bucketId: smallint("bucket_id"),
}).as(sql`SELECT a.bucket_group, max(b.bucket_id) AS bucket_id FROM frs9_param_bucketh a JOIN frs9_param_bucketd b ON a.pkid = b.pkid_header GROUP BY a.bucket_group, a.bucket_default`);

export const vwFrs9MovementSnapshot = pgView("vw_frs9_movement_snapshot", {
	downloadDate: date("download_date"),
	eclModelId: smallint("ecl_model_id"),
	// You can use { mode: "bigint" } if numbers are exceeding js number limitations
	accountId: bigint("account_id", { mode: "number" }),
	groupSegment: text("group_segment"),
	accountStatus: text("account_status"),
	stage: varchar({ length: 5 }),
	impairedFlag: text("impaired_flag"),
	exchangeRate: doublePrecision("exchange_rate"),
	eclAmount: doublePrecision("ecl_amount"),
	grossCarryingAmount: doublePrecision("gross_carrying_amount"),
	restructureFlag: boolean("restructure_flag"),
	pociFlag: boolean("poci_flag"),
}).as(sql`SELECT a.prc_date AS download_date, a.ecl_model_id, a.account_id, lower(a.group_segment::text) AS group_segment, lower( CASE WHEN b.account_id IS NULL THEN 'A'::text ELSE 'W'::text END) AS account_status, a.stage, lower(a.impaired_status::text) AS impaired_flag, COALESCE(a.exchange_rate, 1::double precision) AS exchange_rate, COALESCE(a.ecl_final_amt, 0::numeric)::double precision * COALESCE(a.exchange_rate, 1::double precision) AS ecl_amount, COALESCE(a.carrying_amt, 0::numeric)::double precision * COALESCE(a.exchange_rate, 1::double precision) AS gross_carrying_amount, a.restructure_flag, false AS poci_flag FROM frs9_master_account a LEFT JOIN frs9_master_account_wo b ON b.account_id = a.account_id AND b.prc_date = a.prc_date WHERE lower(a.account_status::text) = 'a'::text OR b.account_id IS NOT NULL`);