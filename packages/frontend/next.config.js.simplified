/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep basic settings from your complex config
  reactStrictMode: true,
  
  // Skip the critters check for now
  experimental: {
    optimizePackageImports: [
      '@mui/material',
      '@mui/icons-material',
      'react-admin',
      'lodash'
    ],
  },

  // Skip complex webpack config for now
  // webpack: your webpack config here if needed

  // Keep compiler options
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production' ? {
      exclude: ['error']
    } : false,
  },

  // Skip complex headers for now
  
  // Skip redirects and rewrites for testing
  
  // Keep TypeScript and ESLint settings
  typescript: {
    ignoreBuildErrors: process.env.NODE_ENV === 'development',
  },
  
  eslint: {
    ignoreDuringBuilds: process.env.NODE_ENV === 'development',
  },

  // Basic performance settings
  poweredByHeader: false,
  compress: true,
};

module.exports = nextConfig;
