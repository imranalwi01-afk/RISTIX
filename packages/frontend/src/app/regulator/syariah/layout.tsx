// packages/frontend/src/app/regulator/syariah/layout.tsx
// ============================================================================
// IFRS9 FRONTEND - REGULATOR SYARIAH LAYOUT
// ============================================================================

import React from 'react';

export default function SyariahLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
