version: "3.8"

services:
  # ============================================================================
  # 🗄️ POSTGRES (OPTIONAL – local / fallback only)
  # ============================================================================
  postgres:
    image: postgres:16-alpine
    container_name: ristix-postgres-prod
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-ifrspro_platform_admin}
    volumes:
      - postgres-prod-data:/var/lib/postgresql/data
    networks:
      - ristix-prod
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles: [ "db", "full" ]

  # ============================================================================
  # 📊 REDIS (OPTIONAL – local / fallback only)
  # ============================================================================
  redis:
    image: redis:7-alpine
    container_name: ristix-redis-prod
    restart: always
    command: redis-server --appendonly yes
    volumes:
      - redis-prod-data:/data
    networks:
      - ristix-prod
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles: [ "db", "full" ]

  # ============================================================================
  # 🚀 BACKEND (PRODUCTION IMAGE)
  # ============================================================================
  backend:
    image: ghcr.io/imranalwi01-afk/ristix/backend:production
    container_name: ristix-backend-prod
    restart: always
    env_file:
      - ./env/backend.env
    ports:
      - "4232:4232"
    networks:
      ristix-prod:
        aliases:
          - new-backend
    profiles: [ "app", "full" ]

  # ============================================================================
  # 🌐 FRONTEND (PRODUCTION IMAGE)
  # ============================================================================
  frontend:
    image: ghcr.io/imranalwi01-afk/ristix/frontend:production
    container_name: ristix-frontend-prod
    restart: always
    env_file:
      - ./env/frontend.env
    environment:
      HOSTNAME: "0.0.0.0"
    ports:
      - "4231:4231"
    networks:
      - ristix-prod
    depends_on:
      - backend
    profiles: [ "app", "full" ]

  # ============================================================================
  # 📈 R ANALYTICS (PRODUCTION IMAGE)
  # ============================================================================
  r-analytics:
    build:
      context: ../../packages/r-analytics
      dockerfile: Dockerfile
    image: ghcr.io/imranalwi01-afk/ristix/r-analytics:production
    container_name: ristix-r-analytics-prod
    restart: always
    env_file:
      - ./env/r-analytics.env
    ports:
      - "4236:3838"
      - "4241:4241"
    healthcheck:
      test: [ "CMD-SHELL", "(curl -fsS http://localhost:4236/ >/dev/null || curl -fsS http://localhost:3838/ >/dev/null) && curl -fsS http://localhost:4241/health >/dev/null" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - ristix-prod
    profiles: [ "analytics", "full" ]

# ============================================================================
# 📁 VOLUMES
# ============================================================================
volumes:
  postgres-prod-data:
  redis-prod-data:

    # ============================================================================
    # 🌐 NETWORKS
    # ============================================================================
networks:
  ristix-prod:
    driver: bridge
